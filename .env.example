# AES-CBC secret key (16 bytes = AES-128, 32 bytes = AES-256)
# In production never hardcode keys — load from environment
CBC_SECRET_KEY=0123456789abcdef0123456789abcdef

# AES-GCM secret key (32 bytes for AES-256)
GCM_SECRET_KEY=fedcba9876543210fedcba9876543210

# Flask settings
FLASK_DEBUG=False